# Спека — Группа A: правки копирайта и лейаута (встреча 2026-06-23)

Первый из трёх циклов разбиения правок встречи 2026-06-23 (A → B → C).
**Группа A** — лёгкие правки без изменения модели данных: welcome-карточки, стоимость каналов, анкета, шаг выбора сценария.
Источник: `2026-06-23-meeting-1423-edits-spec.md` (блоки 1, 5, 2, 3), сверено с кодом ветки `integration`.

Все строки интерфейса — на русском. Накладывается поверх утреннего AIM-батча.

---

## Сквозное правило: навигация «Назад / Далее»

«Назад» — слева, «Далее» / основная кнопка — справа. В визарде уже соблюдено (`StepFooter`). Вне визарда привести к тому же (см. Блок 2, экран подбора сценариев).

---

## Блок 1. Welcome — карточки услуг

**AS IS.** `src/sections/welcome/onboarding-step-cards.tsx`, массив `PLATES` — три карточки: **Сигналы / Кампании / Статистика**. Консьюмер `welcome-view.tsx` рендерит `<OnboardingStepCards />` и копию не хардкодит.

**TO BE.** Средняя карточка — про **коммуникации**, а не кампании. Набор: **Сигналы → Коммуникации → Статистика**.
- Карточка 1 «Сигналы» — текст уже про intent, оставляем без изменений.
- Карточка 2 — заголовок **«Коммуникации»**, описание **«Запускаем нужное сообщение в нужный момент — по выбранным каналам.»**
- Карточка 3 «Статистика» — без изменений.

**Маппинг.** `onboarding-step-cards.tsx` — `PLATES` (источник истины текста). Колокейтед-тест `onboarding-step-cards.test.tsx` синхронизировать с новыми строками.

**Критерии приёмки.**
- Карточки в порядке Сигналы / Коммуникации / Статистика.
- Карточка 2 называется «Коммуникации» и описывает коммуникации.
- `onboarding-step-cards.test.tsx` зелёный.

---

## Блок 5. Каналы — стоимость канала и блок «только сигналы»

**AS IS.** `src/sections/campaigns/wizard/steps/step-channels.tsx`: чекбокс-карточки каналов (`CHANNELS`, подписи `CHANNEL_LABEL`) **без стоимости**; ниже — радио-кнопка **«Не проводить коммуникацию»** без заголовка/описания. Стоимости в `src/sections/campaigns/campaign-cost.ts`: `UNIT_COST` (sms 5, email 1, push 0.5, ivr 8 ₽/отправку); там же `CHANNEL_LABEL`.

**TO BE.**
- В каждую карточку канала — строка стоимости из `UNIT_COST[channel]`, формат **«N ₽ / отправка»** (push → «0,5 ₽ / отправка»).
- Опцию «без коммуникации» вынести **через разделитель** в отдельный блок: заголовок **«Получить только сигналы»**, описание **«Не проводить коммуникации»**. Выбор по-прежнему очищает выбранные каналы.

**Маппинг.**
- `step-channels.tsx`: строка стоимости из `UNIT_COST[channel]`; блок `noComms` отделить разделителем, снабдить заголовком и описанием.
- Источник чисел/подписей — `campaign-cost.ts` (`UNIT_COST`, `CHANNEL_LABEL`), новых чисел не вводить.
- Колокейтед-тест `step-channels.test.tsx` обновить.

**Критерии приёмки.**
- Рядом с каждым каналом — стоимость из `UNIT_COST` в формате «N ₽ / отправка».
- Блок «Получить только сигналы» отделён разделителем, с заголовком и описанием; выбор очищает каналы.

---

## Блок 2. Анкета — порядок полей, валидация «хотя бы одно», кнопка «Назад»

**AS IS.**
- `src/sections/survey/survey-form.tsx`: первым **«Ваша задача»** (textarea, гейт сабмита `isTaskDescriptionValid`), ниже **«Сайт компании»** (input, «Необязательно»).
- `src/state/survey-validation.ts`: экспортирует `isCompanyNameValid`, `isTaskDescriptionValid`. Хелпера «хотя бы одно» нет.
- `src/sections/survey/onboarding-scenarios-screen.tsx` («подобрали N сценариев») — только кнопка **«Далее»** (проп `onChooseScenario`), без «Назад». Рендерится из `survey-section.tsx` (ветка `phase.kind === "scenarios"`, `handleChooseScenario`).

**TO BE.**
- Порядок полей: **«Сайт компании» сверху, «Ваша задача» ниже.** Оба «Необязательно».
- Оба по отдельности необязательны, но **заполнено хотя бы одно**. Пустые оба → ошибка **«Заполните хотя бы одно поле — сайт или задачу»**, переход блокируется.
- На экране подбора сценариев — кнопка **«Назад»** слева, **«Далее»** справа; «Назад» возвращает на форму анкеты.

**Маппинг.**
- `survey-form.tsx`: переставить блоки (сайт выше задачи); гейт сабмита переключить на новый хелпер; ошибку показывать при пустых обоих.
- `survey-validation.ts`: добавить `isSurveyMinimallyFilled({ companyWebsite, taskDescription })` — хотя бы одно непусто после `trim`. `isTaskDescriptionValid` оставить.
- `onboarding-scenarios-screen.tsx`: добавить проп `onBack`, футер «Назад» слева / «Далее» справа.
- `survey-section.tsx`: пробросить `onBack` (возврат на форму анкеты).

**Критерии приёмки.**
- Сайт выше задачи.
- Сабмит проходит при ≥1 заполненном поле; при двух пустых — ошибка и блокировка.
- На экране подбора сценариев есть «Назад» (слева) и «Далее» (справа); «Назад» возвращает на анкету.

---

## Блок 3. Шаг выбора сценария — фильтры сверху, «Показать все» как добавление, тег дубля

**AS IS.** `src/sections/campaigns/wizard/steps/step-1-scenario.tsx`:
- Подборка и полный каталог переключаются **заменой** (`AnimatePresence mode="wait"`).
- Поиск (`Input` + `Search`) и чипсы категорий — только в ветке `showAll`.
- Карточки рендерят `sourceLabel={sourceTypeLabel(s.recommendedSourceType)}`.
- `CURATED_SCENARIOS` — локальная константа в файле (`SCENARIOS.filter((s) => s.isCurated)`); источник истины — флаг `isCurated` в `src/data/scenarios.ts`.
- `src/sections/signals/scenario-card.tsx`: чип источника `sourceLabel?` опционален; бейдж рендерится при `sourceLabel || curatedLabel` (проп `curatedLabel` уже есть).

**TO BE.**
- **Поиск + чипсы категорий — всегда наверху**, над блоком «Подобрали для вас» (вынести из ветки `showAll`).
- **«Показать все» добавляет** каталог под подборкой (не заменяет). «Свернуть» убирает добавленный каталог. Анимацию сжатия/разжатия сохранить, но применять к **добавляемому** блоку.
- **Убрать чип типа источника** с карточек на этом шаге.
- На подобранных карточках — тег-маркер **«Из подборки»** (показывает, что карточка есть и в общем списке). Переиспользовать существующий проп `curatedLabel` в `scenario-card.tsx`.

**Маппинг.**
- `step-1-scenario.tsx`: вынести поиск и чипсы за пределы ветки `showAll`; заменить swap-логику (`mode="wait"`) на добавление (подборка постоянна, при `showAll` ниже разворачивается каталог); убрать передачу `sourceLabel`; для карточек из `CURATED_SCENARIOS` передавать `curatedLabel="Из подборки"`.
- `scenario-card.tsx`: чип источника уже опционален; передавать `curatedLabel` для тега «Из подборки».
- Колокейтед-тест `step-1-scenario.test.tsx` обновить.

**Критерии приёмки.**
- Поиск и фильтр видны всегда, в т.ч. в режиме подборки.
- «Показать все» не прячет подборку, а добавляет каталог ниже; «Свернуть» убирает его.
- На карточках нет чипа типа источника.
- У подобранных карточек есть тег «Из подборки».

---

## Порядок реализации внутри Группы A

1. Блок 1 (welcome-карточки) — изолированный текст + тест.
2. Блок 5 (стоимость каналов) — переиспользует `UNIT_COST`.
3. Блок 2 (анкета) — хелпер валидации + проброс `onBack`.
4. Блок 3 (шаг сценария) — самый объёмный: переход swap → добавление + тег.

Блоки независимы; единственная связь — сквозное правило «Назад слева» (Блок 2).
