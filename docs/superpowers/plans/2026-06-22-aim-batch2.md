# aim Batch 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Implement the 14 aim-queued edits (batch 2): source-label renames, catalog collapse + curated chip, scenario-recommended source, full triggers-step restore, budget channel breakdown + copy, graph-based cost convergence, and the pre-launch signal-collection flow with source-aware launch gating.

**Architecture:** Client-only React/reducer changes on `feature/finish-campaign-first`. Order by risk: light copy/UX first (labels, catalog, budget copy), then medium (source recommendation, graph cost), then the two heavy pieces last (full `step-2-interests` restore; pre-launch collection flow + launch gating, which revises reducer phase semantics).

**Tech Stack:** Next.js 16, TypeScript, Tailwind v4, React reducer state, Vitest.

**Source spec:** `docs/superpowers/specs/2026-06-22-aim-batch2-design.md`.

**⚠️ Environment:** repo dir name has a non-breaking space. Shell: `cd /tmp/afina-wt`. Read/Write/Edit: `/tmp/afina-wt/...`.

**⚠️ Baseline gates (verified):** `npx tsc --noEmit` has exactly **13 pre-existing errors** (12 `src/components/ai-elements/*`, 1 `campaign-cost.ts:154`) — must remain, nothing else. Green = `npx tsc --noEmit 2>&1 | grep -vE "ai-elements|campaign-cost" | grep "error TS"` empty. `npx vitest run` fully green (943) — keep it. Commit with TARGETED `git add` (never `-am` — avoid sweeping the dev-only aim inject in `layout.tsx`).

**⚠️ Verified facts:**
- Source labels live in 5 places (no single source): `step-source.tsx` SOURCE_OPTIONS, `step-1-scenario.tsx` `sourceTypeLabel()`, `campaign-card.tsx` SOURCE_LABEL, `artifact-screen.tsx` SOURCE_LABEL, `artifacts-empty-state.tsx` (prose).
- `getScenario(id)` (`@/data/scenarios`) returns `{ signalType, recommendedSourceType, isCurated, ... }`. Every scenario has `recommendedSourceType`.
- Providers `PromptChipsProvider` / `DraftQueueProvider` / `TriggerEditRegistryProvider` already wrap `renderMain()` in `app/page.tsx:99-103` — the wizard + campaign screen render inside them. **No provider wiring needed for the step-2 restore.**
- `Step2Interests` (`step-2-interests.tsx`) is StepProps-shaped (`{data,onNext,onBack}`), persists `interests`/`triggers`/`triggerConfig` via `onNext`, has `initialPrefill` (seeded by `clientDirection`).
- Cost paths: wizard Budget uses flat `estimateCampaignBudget`; payment screen headline + "Из чего складывается" use graph `computeCampaignCost(graph.nodes, graph.edges, audienceSize)` where `graph = getCachedGraph(id) ?? createTemplate(scenarioSignalType, sourceType)`. `CampaignCost = { total, primary, repeat, hasDynamic, lines: {nodeId,channel,label,unit,reach,sum}[] }`.
- Reducer `campaign_created_from_wizard` currently → `status:"draft"`, `view:{kind:"workflow"}`. `campaign_launched` sets `phase = new?"scoring":"communicating"` + generates artifact for own/stream. `campaign_phase_advanced` sets phase "communicating" + idempotent artifact. campaign-screen has an effect firing `campaign_phase_advanced` after `SCORING_WINDOW_MS=8000` for active non-own scoring campaigns; `CampaignSignalProgress` renders when `isScoring`.

---

## File Structure

**Modified:**
- A: `step-source.tsx`, `step-1-scenario.tsx`, `campaign-card.tsx`, `artifact-screen.tsx`, `artifacts-empty-state.tsx` (+ their tests).
- B: `step-1-scenario.tsx`, `signals/scenario-card.tsx`.
- C: `step-source.tsx`.
- D: `campaign-workspace.tsx` (swap component), delete `step-interests.tsx`.
- E: `step-budget.tsx`.
- F: `step-budget.tsx`, `campaign-payment-screen.tsx` (+ a shared cost helper if extracted).
- G: `app-state.ts` (reducer), `campaign-screen.tsx`, new pure helper `campaign-launch-gate.ts`.

---

## Task 0: Baseline check
- [ ] `cd /tmp/afina-wt && npx tsc --noEmit 2>&1 | grep -vE "ai-elements|campaign-cost" | grep "error TS"` → empty; `npx vitest run 2>&1 | grep -E "Tests " | tail -1` → all pass. If red, STOP.

---

## Task 1: Source-label rename (A, #1/#2/#5)

**Files:** `step-source.tsx`, `step-1-scenario.tsx`, `campaign-card.tsx`, `artifact-screen.tsx`, `artifacts-empty-state.tsx`; tests `scenario-card.test.tsx`, `step-1-scenario.test.tsx`, `artifact-screen.test.tsx`.

Rename: `new` «Новая база» → «Новая база номеров»; `own` «Своя база» → «Свои сигналы»; `stream` «Поток» unchanged. Only labels (not card descriptions).

- [ ] **Step 1:** In each of the 5 files, replace the `new` and `own` label strings. Concretely:
  - `step-source.tsx` SOURCE_OPTIONS: `label: "Новая база"` → `"Новая база номеров"`; `label: "Своя база"` → `"Свои сигналы"`.
  - `step-1-scenario.tsx` `sourceTypeLabel`: `case "new": return "Новая база"` → `"Новая база номеров"`; `case "own": return "Своя база"` → `"Свои сигналы"`.
  - `campaign-card.tsx` SOURCE_LABEL: `new: "Новая база"` → `"Новая база номеров"`, `own: "Своя база"` → `"Свои сигналы"`.
  - `artifact-screen.tsx` SOURCE_LABEL: same two replacements.
  - `artifacts-empty-state.tsx`: prose mentioning «Своя база» → «Свои сигналы» (read the exact sentence and update; it references the source step).
- [ ] **Step 2:** Update tests asserting old strings (do not weaken): `scenario-card.test.tsx` (`/нов/i`, `/сво/i` assertions — replace expected text with new labels), `step-1-scenario.test.tsx` (the `sourceTypeLabel` regex `/нов/i` at ~:30 → match «Новая база номеров»), `artifact-screen.test.tsx:71` (SOURCE_LABEL text). Run `git grep -n "Новая база\|Своя база" -- 'src/**/*.test.*'` to find every assertion.
- [ ] **Step 3:** `cd /tmp/afina-wt && npx tsc --noEmit 2>&1 | grep -vE "ai-elements|campaign-cost" | grep "error TS"` → empty; `npx vitest run 2>&1 | grep -E "Tests " | tail -1` → all pass. Also `git grep -n "Новая база\b\|Своя база\b" -- src | grep -v "номеров\|\.test\."` → should be empty (no stray old labels left; «Новая база номеров» is fine).
- [ ] **Step 4:** Commit `git add` the 5 source files + touched tests; `git commit -m "feat(labels): rename source labels — Новая база номеров / Свои сигналы"`.

---

## Task 2: Catalog collapse-back + curated chip (B, #3)

**Files:** `step-1-scenario.tsx`, `signals/scenario-card.tsx` (+ tests).

- [ ] **Step 1 (scenario-card curated chip):** Add optional prop `curatedLabel?: string` to `ScenarioCard`. When present, render a second compact neutral `Badge variant="secondary"` next to the existing `sourceLabel` badge (same row/area), text = the label. Keep it neutral (not brand-yellow).
- [ ] **Step 2 (collapse-back):** In `step-1-scenario.tsx` expanded view (`showAll`), add a **«Свернуть»** button (alongside/below the category catalog) that calls `setShowAll(false)`, returning to the curated default. (The existing «Показать все» enters expanded; this exits.)
- [ ] **Step 3 (curated chip wiring):** In the expanded view, for each card whose scenario `isCurated`, pass `curatedLabel="Подобрано для вас"`. (Default view shows only curated scenarios, so no chip needed there.)
- [ ] **Step 4:** Update/add `step-1-scenario.test.tsx` + `scenario-card.test.tsx`: card renders the curated badge when `curatedLabel` set; expanded view shows «Свернуть» and curated cards carry «Подобрано для вас»; «Свернуть» returns to default (curated + «Показать все»). Don't weaken selection assertions. (Mock `step-content` as the existing step-1 test does for the typewriter wrapper.)
- [ ] **Step 5:** tsc clean (filtered); full suite green.
- [ ] **Step 6:** Commit `git add` the two files + tests; `git commit -m "feat(catalog): collapse-back + Подобрано для вас chip in full catalog"`.

---

## Task 3: Budget step — channel breakdown + copy (E, #7/#8)

**Files:** `step-budget.tsx`. Read it first (post batch-1 it has `~` formatting, `buildBudgetRows`, custom-sum rescale).

NOTE: This task does the COPY/breakdown only on the CURRENT (flat-estimate) numbers. Task 5 (F) then swaps the underlying numbers to the graph model — keep the channel-list/buffer rendering written so it reads from whatever cost object is in scope (after Task 5 it reads graph `cost`). To avoid churn, implement Task 3 against the current rows, and Task 5 will re-point the data source while keeping this presentation.

- [ ] **Step 1 (channel list):** In the «Коммуникация» area, render the list of used channels from `data.channels` mapped via `CHANNEL_LABEL` (import from `@/sections/campaigns/campaign-cost`). E.g. a muted sub-line «Каналы: SMS, Email». If `data.channels` is empty, show «—» / omit.
- [ ] **Step 2 (repeat buffer line):** Add a muted line «Повторные коммуникации (+30% буфер)» under the communication figure (mirror the payment screen's wording), shown when there is communication cost (channels non-empty).
- [ ] **Step 3 (copy #8):** Change the «По источнику и каналам» caption (on the «Рекомендуемая» card) to «Рассчитали на основе источников и каналов».
- [ ] **Step 4:** Update `step-budget.test.tsx` for the new caption + channel line if asserted; don't weaken.
- [ ] **Step 5:** tsc clean (filtered); full suite green.
- [ ] **Step 6:** Commit `git add src/sections/campaigns/wizard/steps/step-budget.tsx` + test; `git commit -m "feat(budget): communication channel list + repeat-buffer line + caption copy"`.

---

## Task 4: Source step — scenario recommendation (C, #4/#9)

**Files:** `step-source.tsx`.

- [ ] **Step 1:** Import `getScenario` from `@/data/scenarios`. Compute `const recommended = data.scenario ? getScenario(data.scenario)?.recommendedSourceType : undefined`.
- [ ] **Step 2 (prefill):** Initialize the local source selection to the recommendation when the user hasn't chosen yet. Since `data.sourceType` defaults to `"new"`, prefer: `useState<SourceType>(() => recommended ?? data.sourceType)`. (Changing source still recomputes the wizard tail — unchanged behavior from batch 1.)
- [ ] **Step 3 (badge):** On the source card whose `value === recommended`, render a small **«Рекомендуется»** badge (e.g. top-left, neutral or subtle accent — keep subtle, not a yellow fill). Only one card gets it.
- [ ] **Step 4:** Add/extend `step-source.test.tsx`: given `data.scenario` with a known `recommendedSourceType`, the matching card shows «Рекомендуется» and is preselected. (Pick a scenario id whose `recommendedSourceType` is deterministic per `SOURCE_BY_CATEGORY`.)
- [ ] **Step 5:** tsc clean (filtered); full suite green.
- [ ] **Step 6:** Commit `git add src/sections/campaigns/wizard/steps/step-source.tsx` + test; `git commit -m "feat(source): mark + preselect scenario-recommended source"`.

---

## Task 5: Graph-based cost convergence (F, #14)

**Files:** `step-budget.tsx`, `campaign-payment-screen.tsx`. Optionally extract `src/sections/campaigns/campaign-graph-cost.ts` helper.

Goal: both the wizard Budget step and the payment screen derive headline numbers from the SAME graph model (`computeCampaignCost` over `createTemplate(scenarioSignalType, sourceType)` with the same base), so «Коммуникация»/«Итого» match.

- [ ] **Step 1 (shared helper, TDD):** Create `src/sections/campaigns/campaign-graph-cost.ts` exporting `graphCostFor(args: { scenarioId: string | null; sourceType: SourceType; baseSize: number }): CampaignCost | null` — resolves `signalType = getScenario(scenarioId)?.signalType`, builds `createTemplate(signalType, sourceType)`, returns `computeCampaignCost(graph.nodes, graph.edges, baseSize)`; returns null when no signalType. Test `campaign-graph-cost.test.ts`: for a known scenario+source+base, returns a `CampaignCost` with `total > 0` and `lines.length > 0`, and is deterministic (same inputs → same total). Run red → implement → green.
- [ ] **Step 2 (wizard Budget uses it):** In `step-budget.tsx`, replace the `estimateCampaignBudget`-derived rows with rows built from `graphCostFor({ scenarioId: data.scenario, sourceType: data.sourceType, baseSize: data.fileRowCount ?? FALLBACK_BASE })`. Map `CampaignCost` → the existing Сигналы/Коммуникация/Итого rows: Сигналы = scoring portion (own → «бесплатно»/0; else the signal/scoring cost — derive from cost or keep the existing signal line if cost model separates it; if `CampaignCost` has no explicit signals line, show Сигналы from the source-matrix rule: own=0 else a scoring estimate — keep consistent with payment screen's split), Коммуникация = `cost.total - signalsPortion` or the comms lines sum, Итого = `cost.total`. Keep the `~` approx formatting, the channel list + buffer line (Task 3), and the `scaleBreakdown` custom-sum rescale. The contact-count `~N контактов` uses `baseSize`.
  > Judgment point: `CampaignCost` models the whole campaign; align the Сигналы vs Коммуникация split with how the payment screen presents it so both screens agree. Read `campaign-payment-screen.tsx` cost rendering and mirror its split exactly.
- [ ] **Step 3 (payment screen single source):** In `campaign-payment-screen.tsx`, make the «Платежи» block and the «Из чего складывается» block both derive from the SAME `computeCampaignCost` result (currently «Платежи» uses flat `splitCampaignPayments`). Replace the flat split with a graph-derived split so the screen is internally consistent and matches the wizard. Keep the inline shortfall hint + custom-sum rescale from batch 1.
- [ ] **Step 4:** Verify convergence in a test or by reading: for the same scenario/source/base, the wizard `graphCostFor(...).total` equals the payment screen's `recommended` (`cost.total`). Add `campaign-graph-cost.test.ts` assertion that wizard and payment use identical inputs → identical total (test at the helper level).
- [ ] **Step 5:** Remove now-dead `estimateCampaignBudget` / `splitCampaignPayments` usages; if fully unreferenced (`git grep`), delete the functions + their tests; otherwise leave with a note. tsc clean (filtered); full suite green (update budget/payment tests to the graph numbers — don't weaken).
- [ ] **Step 6:** Commit `git add` the helper + step-budget + payment screen + tests; `git commit -m "feat(cost): unify wizard + payment on graph-based campaign cost"`.

---

## Task 6: Restore full interests+triggers step (D, #6/#10)

**Files:** `campaign-workspace.tsx`; delete `step-interests.tsx`. Providers already mounted (verified).

- [ ] **Step 1:** In `campaign-workspace.tsx` `renderStepContent`, change the `"interests"` case from `<StepInterests .../>` to `<Step2Interests {...props} onBack={onBack} />` (import `Step2Interests` from `@/sections/campaigns/wizard/steps/step-2-interests`). Confirm its props match `{data, onNext, onBack}` (StepProps) — read the component's signature and pass exactly what it expects.
- [ ] **Step 2:** Remove the now-unused `StepInterests` import. `git grep -n "step-interests\b\|StepInterests" -- src` — if nothing else references it, `git rm src/sections/campaigns/wizard/steps/step-interests.tsx` (+ its test if present).
- [ ] **Step 3:** Verify `Step2Interests` mounts without provider errors (it uses `usePromptChips`/`useRegisterTriggerEdit` — both providers wrap the wizard in `page.tsx`). If `Step2Interests` reads anything not in `StepData`, reconcile.
- [ ] **Step 4:** Tests: the wizard's interests step now renders triggers. Update any wizard/step test that assumed the light `StepInterests`. Add/confirm a test that the `"interests"` step renders trigger UI for a selected interest (mock `step-content`; the component has heavy deps — if a full render is impractical in jsdom, assert at the `campaign-workspace` renderStepContent mapping level or a focused `Step2Interests` smoke test with the providers wrapped). Don't weaken.
- [ ] **Step 5:** tsc clean (filtered); full suite green.
- [ ] **Step 6:** Commit `git add` campaign-workspace + removed file + tests; `git commit -m "feat(wizard): restore full interests+triggers step (step-2-interests)"`.

---

## Task 7: Pre-launch signal-collection flow + launch gating (G, #11/#12/#13)

**Files:** `app-state.ts` (reducer), `campaign-screen.tsx`, new `src/sections/campaigns/campaign-launch-gate.ts` (pure). This revises the campaign-first phase semantics: collection now happens PRE-launch for `new`.

**Model (prototype):** a `new` campaign collects signals while still `draft` (`phase: "scoring"`); after the window, `campaign_phase_advanced` sets `phase: "communicating"` + generates the collected-signals artifact (idempotent). `stream`/`own` skip collection. «Запустить» (→ payment → `campaign_launched`) is enabled when collection is done (new) or immediately (stream/own), and the payment screen's balance gate (batch 1) blocks launch when insufficient. `campaign_launched` sets `phase: "communicating"` and generates an artifact only if none exists (idempotent) — so own/stream get theirs at launch, new keeps its pre-launch one.

- [ ] **Step 1 (launch-gate helper, TDD):** Create `campaign-launch-gate.ts` exporting `canLaunchCampaign(c: { status: CampaignStatus; sourceType?: SourceType; phase?: "scoring"|"communicating" }): boolean` and `isCollecting(c): boolean`. Rules: `isCollecting` = `status==="draft" && sourceType!=="own" && sourceType!=="stream"... ` — wait, only `new` collects: `isCollecting = status==="draft" && sourceType==="new" && (phase ?? "scoring")==="scoring"`. `canLaunchCampaign` = for draft: `sourceType==="new" ? !isCollecting(c) : true` (stream/own always launchable from draft); paused → true; active/completed → false (already launched). Test `campaign-launch-gate.test.ts`: new draft+scoring → not launchable + collecting; new draft+communicating → launchable + not collecting; stream/own draft → launchable + not collecting; active → not launchable. Run red → implement → green.
- [ ] **Step 2 (reducer: campaign_created_from_wizard):** Change it to route `view:{kind:"campaign", campaign:{id,name}}` (not `workflow`), and set initial phase by source: `new` → `phase:"scoring"` (collecting); `stream`/`own` → no phase. Keep `status:"draft"`. Update `src/state/create-campaign-from-wizard.test.ts` to assert the new view kind + per-source phase. Run.
- [ ] **Step 3 (reducer: campaign_launched phase/artifact):** Change `campaign_launched` so phase becomes `"communicating"` for all sources (collection is pre-launch now), and the artifact is generated only if none exists for the campaign (idempotent guard `state.artifacts.some(a => a.campaignId === id)`) — own/stream generate here, new already has one. Update `artifact-generation.test.ts`: own/stream still get an artifact at launch; a `new` campaign that already has a pre-launch artifact does not get a second; launched phase is `communicating`. Run.
- [ ] **Step 4 (campaign-screen effect + render):** In `campaign-screen.tsx`:
  - Change the auto-advance effect to fire `campaign_phase_advanced` after `SCORING_WINDOW_MS` when the campaign is **collecting** (`status==="draft" && sourceType==="new" && phase==="scoring"`), instead of (or in addition to) the old active-scoring condition. Remove the old post-launch new-scoring path if now redundant (new no longer scores post-launch).
  - Render `CampaignSignalProgress` when `isCollecting(campaign)` (pre-launch, draft). Keep the «сбор сигналов» visual.
  - Gate the «Запустить» button with `canLaunchCampaign(campaign)`: hidden/disabled while collecting; visible+enabled when launchable. Clicking → `open_campaign_payment` (unchanged).
- [ ] **Step 5 (verify reducer chain):** `cd /tmp/afina-wt && npx vitest run src/state 2>&1 | tail -5` → pass. Confirm: wizard finish (new) → draft+scoring+campaign view; after window → campaign_phase_advanced → communicating + artifact; canLaunch true → payment → campaign_launched → active+communicating (no 2nd artifact). For own/stream: wizard finish → draft (no phase) → canLaunch immediately → payment → launched + artifact.
- [ ] **Step 6:** tsc clean (filtered); full suite green. Update any campaign-screen / create-flow / artifact tests touched. Don't weaken.
- [ ] **Step 7:** Commit `git add` app-state.ts + campaign-screen.tsx + campaign-launch-gate.ts + tests; `git commit -m "feat(flow): pre-launch signal collection + source-aware launch gating"`.

---

## Task 8: End-to-end verification

- [ ] **Step 1:** `cd /tmp/afina-wt && npx tsc --noEmit 2>&1 | grep -c "error TS"` → 13; `... | grep -vE "ai-elements|campaign-cost" | grep "error TS"` → empty. `npx vitest run 2>&1 | grep -E "Tests " | tail -1` → all pass.
- [ ] **Step 2 (manual smoke, dev server on :3000 from this worktree):** survey → catalog (curated default, «Показать все» → full categories with «Подобрано для вас» chips → «Свернуть» back) → pick scenario → source step (recommended marked + preselected, label «Новая база номеров»/«Свои сигналы»/«Поток») → interests step shows full triggers UI → channels → budget (channel list + buffer line, «Рассчитали на основе источников и каналов», numbers match payment) → finish → **campaign card opens**: `new` shows signal collection in progress, «Запустить» disabled until done; `stream`/`own` «Запустить» immediately → payment (balance gate, numbers match wizard) → launch. Workflow scoring node still not overlapping (batch 1).
- [ ] **Step 3:** Report branch + commit list. Do not merge (AGENTS.md).

---

## Self-Review notes
- **Spec coverage:** A→T1; B→T2; C→T4; D→T6; E→T3; F→T5; G→T7. (#10 prefill satisfied by T6's `initialPrefill`.)
- **Order:** light (T1-T3) → medium (T4-T5) → heavy (T6-T7). T5 re-points the data the T3 presentation reads — T3 written presentation-only so T5 swaps the source cleanly.
- **Type consistency:** `graphCostFor` returns `CampaignCost` (same type the payment screen uses); `canLaunchCampaign`/`isCollecting` consume `{status,sourceType,phase}`; reducer phase becomes "communicating" at launch across sources.
- **Risk / judgment points flagged inline:** the Сигналы/Коммуникация split mapping in T5 Step 2 (mirror payment screen), and the phase-semantics revision in T7 (collection moves pre-launch). Both are called out for the implementer to reconcile against the real payment-screen rendering.
- **Out of scope (per spec):** wiring user-selected channels into the graph template (graph channels stay scenario-driven); known simplification noted.
